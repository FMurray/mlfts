{"prompt": "Implement the following plan:\n\n# Plan: Add Git SHA Auto-Tagging + `/tag` and `/feedback` Slash Commands\n\n## Context\n\nThe Claude Code MLflow plugin currently logs traces when a conversation ends (via the Stop hook), but provides n...